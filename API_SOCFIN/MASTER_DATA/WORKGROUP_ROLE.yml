info:
  name: WORKGROUP_ROLE
  type: http
  seq: 31

http:
  method: GET
  url: "{{base_url_socfin}}/masterupkeep/workgroup-role/TG"
  auth:
    type: apikey
    key: Authorization
    value: "{{token}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
