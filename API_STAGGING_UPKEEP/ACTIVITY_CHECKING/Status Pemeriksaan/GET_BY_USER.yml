info:
  name: GET_BY_USER
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/status-activity-checking/get-by-user"
  body:
    type: multipart-form
    data:
      - name: EstateCode
        type: text
        value: AL
      - name: date
        type: text
        value: 2026-04-24
      - name: MandorEmployeeNumber
        type: text
        value: "2011804"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
