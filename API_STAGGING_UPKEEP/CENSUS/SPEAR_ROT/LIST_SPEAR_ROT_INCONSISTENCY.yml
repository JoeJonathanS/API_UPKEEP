info:
  name: LIST_SPEAR_ROT_INCONSISTENCY
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/api/cencus/spear-rot/get-inconsistency"
  body:
    type: multipart-form
    data:
      - name: EstateCode
        type: text
        value: TG
      - name: DivisionCode
        type: text
        value: Div.III
      - name: date
        type: text
        value: 2025-03-03
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
