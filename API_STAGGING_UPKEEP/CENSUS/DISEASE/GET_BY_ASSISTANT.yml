info:
  name: GET_BY_ASSISTANT
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/api/cencus/disease/get-by-assistant"
  body:
    type: multipart-form
    data:
      - name: EstateCode
        type: text
        value: MP
      - name: DivisionCode
        type: text
        value: Div.I
      - name: date
        type: text
        value: 2025-10-03
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
