info:
  name: GET_FOR_WORKER
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/cencus/black-bunch/get-by-worker"
  body:
    type: multipart-form
    data:
      - name: EstateCode
        type: text
        value: AL
        description: estate code
      - name: WorkingDivisionCode
        type: text
        value: Div.I
        description: WorkingDivisionCode
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
