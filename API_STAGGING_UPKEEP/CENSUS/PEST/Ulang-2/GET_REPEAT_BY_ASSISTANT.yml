info:
  name: GET_REPEAT_BY_ASSISTANT
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/api/cencus/pest/get-repeat-2-by-assistant"
  body:
    type: multipart-form
    data:
      - name: EstateCode
        type: text
        value: MP
      - name: WorkingDivisionCode
        type: text
        value: Div.III
      - name: date
        type: text
        value: 2025-06-16
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
