info:
  name: GET_BY_ASSISTANT
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/api/cencus/pest/get-by-assistant"
  body:
    type: multipart-form
    data:
      - name: EstateCode
        type: text
        value: MP
      - name: WorkingDivisionCode
        type: text
        value: Div.III
      - name: date
        type: text
        value: 2025-08-19
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
