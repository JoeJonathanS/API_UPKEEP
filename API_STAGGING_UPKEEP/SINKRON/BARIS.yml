info:
  name: BARIS
  type: http
  seq: 36

http:
  method: GET
  url: "{{base_url}}/api/sync/master-baris/?EstateCode=MP&DivisionCode=Div.I"
  params:
    - name: EstateCode
      value: MP
      type: query
    - name: DivisionCode
      value: Div.I
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
