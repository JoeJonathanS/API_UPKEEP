info:
  name: BLOCK
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/sync/master-block/?request_date=2026-01-03&EstateCode=BB"
  params:
    - name: request_date
      value: 2026-01-03
      type: query
    - name: EstateCode
      value: BB
      type: query
  body:
    type: multipart-form
    data:
      - name: worker_id
        type: text
        value: "1"
        disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
