info:
  name: HOLIDAY
  type: http
  seq: 14

http:
  method: GET
  url: "{{base_url}}/api/sync/master-holiday?EstateCode=AL"
  params:
    - name: EstateCode
      value: AL
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
