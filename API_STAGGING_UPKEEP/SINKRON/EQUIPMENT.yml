info:
  name: EQUIPMENT
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/api/sync/master-equipment?EstateCode=NL"
  params:
    - name: EstateCode
      value: NL
      type: query
      description: Mandatory
  body:
    type: multipart-form
    data:
      - name: worker_id
        type: text
        value: "1"
        disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
