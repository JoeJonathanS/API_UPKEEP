info:
  name: LEAVE_BALANCE
  type: http
  seq: 22

http:
  method: GET
  url: "{{base_url}}/api/sync/master-leave-balance/?EstateCode=BB"
  params:
    - name: DivisionID
      value: ""
      type: query
      disabled: true
    - name: EstateCode
      value: BB
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
