info:
  name: EMPLOYEE_NON_WORKGROUP
  type: http
  seq: 32

http:
  method: GET
  url: "{{base_url}}/api/sync/master-employee-non-workgroup/?EstateCode=TB"
  params:
    - name: EstateCode
      value: TB
      type: query
    - name: DivisionCode
      value: Div.I
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
