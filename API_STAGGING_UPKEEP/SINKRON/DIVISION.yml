info:
  name: DIVISION
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/sync/master-division?EstateCode=AP"
  params:
    - name: EstateCode
      value: AP
      type: query
      description: Mandatory
  body:
    type: multipart-form
    data:
      - name: worker_id
        type: text
        value: "1"
        disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
