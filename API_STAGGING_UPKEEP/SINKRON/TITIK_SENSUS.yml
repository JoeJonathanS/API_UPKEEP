info:
  name: TITIK_SENSUS
  type: http
  seq: 33

http:
  method: GET
  url: "{{base_url}}/api/sync/master-titik-sensus/?EstateCode=AL"
  params:
    - name: EstateCode
      value: AL
      type: query
    - name: DivisionCode
      value: Div.III
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
