info:
  name: LEAVE_ELIGIBILITY
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/api/sync/master-leave-eligibility?EstateCode=TB"
  params:
    - name: EstateCode
      value: TB
      type: query
    - name: EmployeeCategory
      value: Pegawai
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
