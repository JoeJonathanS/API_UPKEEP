info:
  name: ALLOCATION_BLOCK
  type: http
  seq: 30

http:
  method: GET
  url: "{{base_url}}/api/sync/master-allocation-block/?EstateCode=TB"
  params:
    - name: DivisionCode
      value: Div.I
      type: query
      disabled: true
    - name: EstateCode
      value: TB
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
