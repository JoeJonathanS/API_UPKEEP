info:
  name: OVERTIME_BALANCE
  type: http
  seq: 23

http:
  method: GET
  url: "{{base_url}}/api/sync/master-overtime-balance/?EstateCode=AL"
  params:
    - name: EstateCode
      value: AL
      type: query
      description: Mandatory
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
