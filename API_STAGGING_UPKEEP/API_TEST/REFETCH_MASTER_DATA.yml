info:
  name: REFETCH_MASTER_DATA
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/refetch-master-data/MP"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
