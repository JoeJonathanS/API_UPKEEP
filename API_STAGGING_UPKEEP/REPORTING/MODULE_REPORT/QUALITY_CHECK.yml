info:
  name: QUALITY_CHECK
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/api/report-per-module/report-quality-check"
  body:
    type: multipart-form
    data:
      - name: EstateCode
        type: text
        value: AL
      - name: DivisionCode
        type: text
        value: Div.I
      - name: TransactionDate
        type: text
        value: 2025-07-15
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
