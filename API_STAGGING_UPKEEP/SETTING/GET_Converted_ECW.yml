info:
  name: GET_Converted_ECW
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/api/get-converted-ECW"
  body:
    type: multipart-form
    data:
      - name: EstateCode
        type: text
        value: SG
      - name: DivisionCode
        type: text
        value: Div.III
      - name: BlockCode
        type: text
        value: 047/92
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
