info:
  name: CENCUS_SETTING
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/api/setting/get-cencus-setting"
  body:
    type: multipart-form
    data:
      - name: EstateCode
        type: text
        value: AP
      - name: date
        type: text
        value: 2025-04-28
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
